// Import the configured Express app from src/app.js
const app = require('./src/app');

// The server is already started in src/app.js
// This file now just exports the app for testing purposes
module.exports = app; 